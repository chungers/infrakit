version: '3.1'

services:
  test:
    build:
      context: ..
      dockerfile: tests/files/Dockerfile
    volumes:
    - /usr/bin/docker:/usr/bin/docker
    - /var/lib/docker:/var/lib/docker
    - /var/run/docker.sock:/var/run/docker.sock
